name: sandbox_test
description: Integration tests for Sandbox

version: 1.0.0
environment:
  sdk: ">=3.3.2 <4.0.0"

publish_to: none
dependencies:
  flutter:
    sdk: flutter
  test: ^1.25.7
  sandbox:
    path: ../ # Path to the main project
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  bdd_widget_test: ^1.7.6
  firebase_core: ^3.5.0
  hooks_riverpod: any
  easy_localization: ^3.0.7
  shared_preferences: ^2.5.3
